{
  "name": "cleango-monorepo",
  "private": true,
  "scripts": {
    "predev": "npx kill-port 5001 5173",
    "dev": "concurrently --kill-others-on-fail false --names \"frontend,backend\" --prefix-colors \"cyan,magenta\" \"npm --prefix frontend run dev\" \"npm --prefix backend run dev\"",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "check": "node dev-check.js"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}